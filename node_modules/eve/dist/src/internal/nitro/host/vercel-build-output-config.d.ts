export declare function createEveVercelOptions(enabled: boolean): {
    config: {
        version: 3;
        framework: {
            slug: string;
            version: string;
        };
    };
} | undefined;
