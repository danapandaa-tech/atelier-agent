import type { SandboxCreateOptions } from "#compiled/@vercel/sandbox/index.js";
export declare function getVercelSandboxFetch(createOptions: SandboxCreateOptions): typeof globalThis.fetch;
export declare function getVercelSandboxCredentials(createOptions: SandboxCreateOptions): Promise<VercelSandboxCredentials>;
export interface VercelSandboxCredentials {
    readonly projectId: string;
    readonly teamId: string;
    readonly token: string;
}
